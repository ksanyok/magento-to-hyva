<?php
/**
 * FTC Cashmere — Hyvä Customer Login Form
 * Replaces Luma validation/trim-input with Alpine.js + HTML5 validation
 */
declare(strict_types=1);

use Hyva\Theme\Model\ViewModelRegistry;
use Hyva\Theme\ViewModel\HeroiconsOutline;
use Magento\Customer\Block\Form\Login;
use Magento\Framework\Escaper;

/** @var Login $block */
/** @var Escaper $escaper */
/** @var ViewModelRegistry $viewModels */
?>

<div class="block block-customer-login max-w-md mx-auto"
     x-data="{ showPassword: false, loading: false }">
    <div class="block-title mb-8">
        <h2 class="section-title text-center">
            <?= $escaper->escapeHtml(__('Login')) ?>
        </h2>
    </div>
    <div class="block-content">
        <form class="form form-login"
              action="<?= $escaper->escapeUrl($block->getPostActionUrl()) ?>"
              method="post"
              id="login-form"
              @submit="loading = true">
            <?= $block->getBlockHtml('formkey') ?>
            <fieldset class="fieldset login space-y-6">
                <!-- Email -->
                <div class="field email">
                    <label class="block text-sm uppercase tracking-wider mb-2" for="email">
                        <?= $escaper->escapeHtml(__('Email')) ?> <span class="text-secondary">*</span>
                    </label>
                    <input name="login[username]"
                           value="<?= $escaper->escapeHtmlAttr($block->getUsername()) ?>"
                           id="email"
                           type="email"
                           required
                           <?php if ($block->isAutocompleteDisabled()) : ?>autocomplete="off"<?php endif; ?>
                           title="<?= $escaper->escapeHtmlAttr(__('Email')) ?>"
                           class="w-full py-3 px-4 border border-dark text-base
                                  placeholder-dark-lightest focus:outline-none focus:border-primary
                                  transition-colors duration-DEFAULT"
                           placeholder="<?= $escaper->escapeHtmlAttr(__('your@email.com')) ?>"
                    />
                </div>

                <!-- Password -->
                <div class="field password">
                    <label class="block text-sm uppercase tracking-wider mb-2" for="pass">
                        <?= $escaper->escapeHtml(__('Password')) ?> <span class="text-secondary">*</span>
                    </label>
                    <div class="relative">
                        <input name="login[password]"
                               :type="showPassword ? 'text' : 'password'"
                               id="pass"
                               required
                               <?php if ($block->isAutocompleteDisabled()) : ?>autocomplete="off"<?php endif; ?>
                               title="<?= $escaper->escapeHtmlAttr(__('Password')) ?>"
                               class="w-full py-3 px-4 pr-12 border border-dark text-base
                                      focus:outline-none focus:border-primary
                                      transition-colors duration-DEFAULT"
                        />
                        <button type="button"
                                class="absolute right-3 top-1/2 -translate-y-1/2 text-dark-lightest
                                       hover:text-dark transition-colors cursor-pointer"
                                @click="showPassword = !showPassword"
                                :aria-label="showPassword
                                    ? '<?= $escaper->escapeJs(__('Hide password')) ?>'
                                    : '<?= $escaper->escapeJs(__('Show password')) ?>'">
                            <svg x-show="!showPassword" class="w-5 h-5" fill="none" viewBox="0 0 24 24"
                                 stroke="currentColor" stroke-width="1.5">
                                <path stroke-linecap="round" stroke-linejoin="round"
                                      d="M2.036 12.322a1.012 1.012 0 010-.639C3.423 7.51 7.36 4.5 12 4.5c4.638
                                         0 8.573 3.007 9.963 7.178.07.207.07.431 0 .639C20.577 16.49 16.64 19.5
                                         12 19.5c-4.638 0-8.573-3.007-9.963-7.178z"/>
                                <path stroke-linecap="round" stroke-linejoin="round"
                                      d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                            </svg>
                            <svg x-show="showPassword" x-cloak class="w-5 h-5" fill="none" viewBox="0 0 24 24"
                                 stroke="currentColor" stroke-width="1.5">
                                <path stroke-linecap="round" stroke-linejoin="round"
                                      d="M3.98 8.223A10.477 10.477 0 001.934 12c1.292 4.338 5.31 7.5 10.066
                                         7.5.993 0 1.953-.138 2.863-.395M6.228 6.228A10.45 10.45 0 0112 4.5c4.756
                                         0 8.773 3.162 10.065 7.498a10.523 10.523 0 01-4.293 5.774M6.228
                                         6.228L3 3m3.228 3.228l3.65 3.65m7.894 7.894L21 21m-3.228-3.228l-3.65-3.65m0
                                         0a3 3 0 10-4.243-4.243m4.242 4.242L9.88 9.88"/>
                            </svg>
                        </button>
                    </div>
                </div>

                <?= $block->getChildHtml('form_additional_info') ?>

                <!-- Actions -->
                <div class="actions-toolbar flex flex-col gap-4 pt-2">
                    <button type="submit"
                            class="btn-primary w-full py-4"
                            :disabled="loading"
                            name="send"
                            id="send2">
                        <span x-show="!loading"><?= $escaper->escapeHtml(__('Sign In')) ?></span>
                        <span x-show="loading" x-cloak class="inline-flex items-center gap-2">
                            <svg class="animate-spin h-4 w-4" viewBox="0 0 24 24" fill="none">
                                <circle class="opacity-25" cx="12" cy="12" r="10"
                                        stroke="currentColor" stroke-width="4"></circle>
                                <path class="opacity-75" fill="currentColor"
                                      d="M4 12a8 8 0 018-8v4a4 4 0 00-4 4H4z"></path>
                            </svg>
                            <?= $escaper->escapeHtml(__('Signing in...')) ?>
                        </span>
                    </button>
                    <a class="text-center text-sm text-dark-lighter hover:text-primary
                              transition-colors duration-DEFAULT"
                       href="<?= $escaper->escapeUrl($block->getForgotPasswordUrl()) ?>">
                        <?= $escaper->escapeHtml(__('Forgot Your Password?')) ?>
                    </a>
                </div>
            </fieldset>
        </form>
    </div>
</div>
